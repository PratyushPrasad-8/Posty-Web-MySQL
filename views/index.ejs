<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posty WEB</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>f
    <h1>Posty WEB</h1>
    <% for(post of posts){ %>
        <div class="post">
            <h3 class="username">@<%= post.username %></h3>
            <p class="content">
                <%= post.content %>
            </p>

            <a href="http://localhost:8080/posts/update/<%= post.id %>"><button>Edit</button></a>
            <a href="http://localhost:8080/posts/view/<%= post.id %>"><button>View</button></a>
            <a href="http://localhost:8080/posts/delete/<%= post.id %>" class="delBtn"><button>Delete</button></a>
        </div>
    <% } %>

    <form method="get" action="/posts/new">
        <button> Add New Post</button>
    </form>
</body>
</html>